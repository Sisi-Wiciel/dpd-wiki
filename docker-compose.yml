services:
  wiki:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dpd-wiki
    restart: unless-stopped
    ports:
      - "3001:80"
    labels:
      - "com.df师.sns=subdomain:wiki"
